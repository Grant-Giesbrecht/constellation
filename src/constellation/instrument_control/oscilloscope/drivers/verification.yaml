# Hardware-verification records for the oscilloscope drivers in this directory.
#
# WHAT THIS FILE IS: a record of which driver methods have actually been exercised against a
# physical instrument, on which model, and how thoroughly. It is written by the hardware test
# suite (`pytest -m hardware ...`), not maintained by hand - see docs/hardware_verification.md.
#
# WHAT IT IS NOT: a record of what the hardware can do. That lives in the driver source, as
# @feature_unavailable / @feature_unimplemented decorators, and is never restated here. A test
# fails if it is.
#
# STATUS VALUES
#   unverified  implemented, never checked against hardware.
#   roundtrip   set/read-back agreed on real hardware. Proves the set/get pair is
#               SELF-CONSISTENT - not that it controls the parameter it claims to. A driver whose
#               setter and getter both address the timebase will round-trip volts/div perfectly.
#   confirmed   a person watched the instrument and confirmed the physical effect. The only
#               status available to action commands (run/stop, single trigger), which have no
#               read-back at all.
#   failed      checked and did not work. Kept deliberately: "we tried and it broke" is far more
#               useful than "nothing is known", and it is not outranked by weaker records.
#
# Records are a LIST per method, keyed by model, because one driver covers a series: a DS1052E
# and a DS1054Z share RigolDS1000Z, and a command can work on one and not the other.

RigolDS1000Z:
  add_measurement:
    - {status: unverified}
  clear_measurements:
    - {status: unverified}
  do_force_trigger:
    - {status: unverified}
  do_single_trigger:
    - {status: unverified}
  get_bandwidth_limit:
    - {status: unverified}
  get_chan_enable:
    - {status: unverified}
  get_coupling:
    - {status: unverified}
  get_div_time:
    - {status: unverified}
  get_div_volt:
    - {status: unverified}
  get_measurement:
    - {status: unverified}
  get_measurement_stat_display:
    - {status: unverified}
  get_offset_time:
    - {status: unverified}
  get_offset_volt:
    - {status: unverified}
  get_probe_attenuation:
    - {status: unverified}
  get_trigger_level:
    - {status: unverified}
  get_trigger_mode:
    - {status: unverified}
  get_trigger_source:
    - {status: unverified}
  get_waveform:
    - {status: unverified}
  run_acquisition:
    - {status: unverified}
  set_bandwidth_limit:
    - {status: unverified}
  set_chan_enable:
    - {status: unverified}
  set_coupling:
    - {status: unverified}
  set_div_time:
    - {status: unverified}
  set_div_volt:
    - {status: unverified}
  set_measurement_stat_display:
    - {status: unverified}
  set_offset_time:
    - {status: unverified}
  set_offset_volt:
    - {status: unverified}
  set_probe_attenuation:
    - {status: unverified}
  set_trigger_level:
    - {status: unverified}
  set_trigger_mode:
    - {status: unverified}
  set_trigger_source:
    - {status: unverified}
  stop_acquisition:
    - {status: unverified}

RigolDS1000E:
  get_chan_enable:
    - {status: unverified}
  get_div_volt:
    - {status: unverified}
  get_offset_volt:
    - {status: unverified}
  get_waveform:
    - {status: unverified}
  set_chan_enable:
    - {status: unverified}
  set_div_volt:
    - {status: unverified}
  set_offset_volt:
    - {status: unverified}
